# PlayLog 产品文档

> 最后更新：2026-05-14（视觉升级 · 纸质手帐风格迭代）

---

## 产品定位

**游戏手帐小程序** —— 用手帐的美学语言，做游戏玩家的生活记录本。

目标用户是有记录习惯的游戏玩家，覆盖手游（王者荣耀）、主机（Switch / Steam）等平台。盲盒、文具、游戏周边作为「收藏」的子集收入收藏柜，是游戏生活的延伸，而非并列的独立模块。

核心差异化：市面上游戏记录工具（Backloggd、GameTrack）偏工具感且为英文；手帐工具（手帐星球）完全不懂游戏圈语言。PlayLog 填补这个空白。

---

## 核心功能：两种分享卡片

### 单次记录卡片（日常动作）

每次玩完游戏，记录一条「今日碎片」后一键生成。内容包含：游戏名、平台、心情标签、当次文字记录、当前进度。风格偏手帐感（便利贴 / 拍立得），随时可发朋友圈或小红书。**频率高，是用户每天打开 PlayLog 的理由。**

### 年度游戏报告（年底爆发）

类似 Spotify Wrapped，汇总全年数据：游玩款数、总时长、心情分布、月度热力图、Top 3 游戏，最终生成玩家人格标签（治愈系 / 肝帝 / 收集癖 / 云玩家等）。**一年一次，是年底的传播钩子和拉新手段。**

---

## 技术栈

- 原生微信小程序 + TypeScript
- 微信云开发（云数据库 + 云存储 + 云函数）
- 游戏数据源：RAWG API（搜索游戏元数据，云函数 `searchGames` 已编写，待部署）
- 分享图生成：微信小程序 Canvas API

---

## 产品路线图

### 第一阶段：核心闭环（让产品能用）

目标：打通「记录一条游玩日志 → 生成分享卡片」最短路径。

- [ ] **云开发环境初始化**：在微信开发者工具开通云开发，创建环境，将环境 ID 填入 `miniprogram/app.ts` 的 `wx.cloud.init({ env: 'YOUR_ENV_ID' })`
- [ ] **云数据库设计**：创建 `playLogs`、`games`、`collectionItems` 三个集合，替换 `utils/mock.ts` 中的静态数据
- [ ] **新建游玩记录表单**：今日页「新建游玩记录」按钮跳转表单页，支持选择游戏、填心情标签、写文字、上传截图，保存至云数据库
- [ ] **今日碎片列表真实化**：从云数据库读取当日记录，替换 mock 数据展示
- [ ] **单次记录分享卡片**：用 Canvas 渲染手帐风卡片（游戏名 + 心情标签 + 文字 + 进度），支持保存到相册
- [ ] **游戏库筛选逻辑**：filter bar 的「在玩 / 想玩 / 已通关 / 搁置」真正绑定过滤事件
- [ ] **游戏卡片编辑**：点击游戏卡片可编辑状态、进度、备注，保存至云数据库
- [ ] **部署 searchGames 云函数**：见下方「待解决问题」

### 第二阶段：体验打磨（让产品好用）

- [ ] 游戏详情页：每款游戏可关联多条游玩记录，展示「游玩时间线」
- [ ] 图片上传：今日碎片支持贴游戏截图，图片存云存储，数据库只存 fileID
- [ ] 分享卡片多模板：支持 2-3 套风格切换（手帐风 / 像素风 / 简约风）
- [ ] 收藏柜增强：盲盒系列完整度追踪（如「夏日系列 5/6，还差隐藏款」），补充「是否隐藏款」「入手价格/渠道」「库存数量」「是否开封」等字段
- [ ] 统计数据真实化：今日页 StatCard 从云数据库实时计算

### 第三阶段：年底爆发（年度报告）

- [ ] 全年数据汇总：统计游玩款数、总时长（需在记录时存入时长字段）、心情分布、月度游玩天数
- [ ] 月度热力图：12 个月的游玩活跃度可视化
- [ ] 玩家人格标签：根据心情分布自动生成（治愈系 / 肝帝 / 收集癖 / 云玩家）
- [ ] 年度报告卡片：Canvas 渲染年度总结分享图，风格参考 Spotify Wrapped（深绿底色 + 金色数据）
- [ ] 「我的」页面入口：年底在 profile 页展示年度报告生成入口

### 商业化路径（副业收益）

- [ ] 分享卡片付费模板：免费 1-2 套基础模板，付费解锁像素风、赛博风、节日限定款
- [ ] 年度报告高级版：免费版基础数据，付费版解锁详细分析 + 精美卡片样式
- [ ] 收藏柜数据库合作：盲盒系列数据维护成熟后，可考虑与盲盒社区合作

---

## 已完成

- [x] 四个 Tab 静态骨架搭建（今日 / 游戏库 / 收藏柜 / 我的）
- [x] 数据模型设计（`GameEntry`、`PlayLog`、`CollectionItem`、`StatCard`、`GameSearchResult`）
- [x] UI 风格定位（手帐感 + 治愈绿，`#6D9F7A` 主色）
- [x] RAWG 游戏搜索云函数编写（`cloudfunctions/searchGames/index.js`）
- [x] 游戏库搜索 UI 及前端逻辑（调用云函数、结果展示、加入书架）
- [x] `app.ts` 添加 `wx.cloud.init` env 占位符（待填入真实环境 ID）
- [x] 全局视觉升级 v1：主色换为薄荷绿 `#3ECF7A`，卡片系统重构，线条插画装饰元素精细化，整体风格更清新现代
- [x] 全局视觉升级 v2（拼贴手帐风）：参考喜茶小程序美学，引入纸张底色 `#FAF8F3`、印章标签系统（方角+双框线+微旋转）、胶带装饰、回形针插画、封面线条小人、虚线分割线；四个页面（今日 / 游戏库 / 收藏柜 / 我的）样式全部更新
- [x] 底色系统升级 v3：全局底色从暖米白 `#FAF8F3` 换为冷白 `#F5F5F5`，卡片背景统一为纯白 `#FFFFFF`，边框/分割线改为中性灰系（`#EBEBEB` / `#E0E0E0`），去除棕黄色调阴影，整体更干净现代
- [x] 文艺风格升级 v4：① 莫兰迪配色（主色 `#5BAF85`、橙 `#D4896A`、黄 `#C9A84C`，降饱和度）② 排版疏朗化（字间距 +2rpx、行高 1.75、section 间距加大）③ 标签/印章全部加 `font-style: italic` 手写感 ④ section 标题改竖线装饰 ⑤ 游戏封面加胶片叠色（暖色渐变 + mix-blend-mode: multiply）⑥ 我的页面工具列表加左侧竖线装饰
- [x] 喜茶平面直角风格 v5：参考喜茶点单页设计语言，全面去圆角（border-radius→0）、去阴影（box-shadow→none）、去卡片背景色块，改用 1rpx 细线（`#EBEBEB`）划分层级；筛选栏/分类 Tab 改为底部下划线激活态；按钮改直角黑底；今日碎片改左侧彩色竖条；收藏柜网格改 1rpx 间距网格线；保留印章标签小圆角+斜体、胶片叠色两个手帐特色元素

---

## 待解决问题

### 🔴 searchGames 云函数部署

**状态：** 待完成（网络问题暂缓）

**步骤：**
1. 开通微信云开发，创建环境，拿到环境 ID
2. 将 `app.ts` 中 `YOUR_ENV_ID` 替换为真实环境 ID
3. 申请 RAWG API Key（需访问 https://rawg.io，国内需梯子）
4. 在云开发控制台 → 云函数 → `searchGames` → 配置 → 环境变量，添加 `RAWG_API_KEY`
5. 右键 `cloudfunctions/searchGames` → 在终端中打开 → `npm install`
6. 右键该文件夹 → 「上传并部署：云端安装依赖」

**备选方案：** 若 RAWG 长期无法访问，可替换为 IGDB（需 Twitch 账号，免费）或 Steam 公开接口（无需注册，适合 Steam 游戏搜索）。

---

## 数据模型（当前版本）

```typescript
// 游戏条目
type GameEntry = {
  id: string;
  title: string;
  platform: 'switch' | 'steam' | 'mobile' | 'other';
  status: 'playing' | 'wishlist' | 'finished' | 'paused';
  accentColor: string;
  progress: string;
  note: string;
  tags: string[];
};

// 游玩日志（待补充：duration 时长字段，imageFileId 截图字段）
type PlayLog = {
  id: string;
  gameTitle: string;
  dateLabel: string;
  mood: '治愈' | '上头' | '想重开' | '欧了' | '肝完';
  summary: string;
  detail: string;
};

// 收藏条目（待补充：isHidden 是否隐藏款，price 入手价格，stock 库存数量，opened 是否开封）
type CollectionItem = {
  id: string;
  name: string;
  series: string;
  category: '盲盒' | '文具' | '周边';
  status: '已拥有' | '心愿' | '重复';
  accentColor: string;
};
```
